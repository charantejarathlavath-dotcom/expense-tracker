This directory holds db.json, the local JSON-file database created on first
run (see server/src/db.js). The generated file is gitignored — only this
placeholder is tracked so the folder exists after cloning.
